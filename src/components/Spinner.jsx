import { Spin } from 'antd';
import React from 'react';

const Spinner = () => {
    return (
        <div className='_spinner'>
            <Spin size='large'/>
        </div>
    );
};

export default Spinner;